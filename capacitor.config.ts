import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.minimalist.shinytracker',
  appName: 'MinimalistShinyTracker',
  webDir: 'dist'
};

export default config;
